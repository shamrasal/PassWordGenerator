import React from 'react';
import { 
  StyleSheet, 
  View,
  Text,
} from 'react-native';
import GeneratePassword from './GeneratePassword';

const App = () => {
  return (
    <View>
      <GeneratePassword/>
    </View>
  );
}

const styles = StyleSheet.create({})

export default App;
